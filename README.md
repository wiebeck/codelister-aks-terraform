# About

Azure Kubernetes Deployment with Terraform.

# Prerequisites

- Azure account (`az login`)
- Permission to create role assignments on "Core Team" subscription (e.g. be "Owner")
- GitHub account with personal access token

# How

Copy file `terraform.tfvars.sample` to `terraform.tfstate` and fill values.